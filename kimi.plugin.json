{
  "name": "intermap",
  "version": "0.1.7",
  "description": "Project-level code mapping: project registry, call graphs, architecture analysis, agent overlay, cross-project dependencies, pattern detection, live changes. MCP server with 9 tools.",
  "license": "MIT",
  "author": "mistakeknot",
  "interface": {
    "displayName": "intermap",
    "shortDescription": "Project-level code mapping: project registry, call graphs, architecture analysis, agent overlay, cross-project depend..."
  },
  "mcpServers": {
    "intermap": {
      "command": "./bin/launch-mcp.sh",
      "env": {
        "INTERMUTE_URL": "http://127.0.0.1:7338",
        "PYTHONPATH": "${KIMI_PLUGIN_ROOT}/python",
        "MCP_TOOL_PROFILE": "core"
      }
    }
  }
}
